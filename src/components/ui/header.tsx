import React from "react";

// Empty header shell. Fill with nav, logo, and actions later.
export function Header() {
  return (
    <header className="w-full border-b bg-white/60 dark:bg-gray-950/60 backdrop-blur supports-[backdrop-filter]:bg-white/40">
      <div className="mx-auto h-14 max-w-7xl px-4 sm:px-6 lg:px-8 flex items-center justify-between">
        {/* TODO: add logo / nav / actions */}
      </div>
    </header>
  );
}
